{
  "name": "wp-dev-env",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint:php": "composer lint && composer format",
    "lint:js": "wp-scripts format-js src/**/*.js",
    "lint:css": "wp-scripts lint-style src/**/*.css --fix",
    "lint": "run-p lint:*"
  },
  "devDependencies": {
    "@wordpress/scripts": "14.0.1",
    "npm-run-all": "4.1.5"
  }
}
